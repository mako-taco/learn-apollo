{
  "dev": {
    "driver": "pg",
    "user": { "ENV": "DB_USER" },
    "password": { "ENV": "DB_PASSWORD" },
    "host": "localhost",
    "database": "store",
    "schema": { "ENV": "DB_SCHEMA" }
  }
}
